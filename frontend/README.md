# GroundVault Frontend

Vite + React + TypeScript + Tailwind app generated by Lovable from the Stitch designs.

Paste the Lovable export into this directory. Expected layout (per `docs/frontend-spec.md`):

```
frontend/
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.ts
├── index.html
├── src/
│   ├── App.tsx
│   ├── main.tsx
│   ├── routes/
│   ├── components/
│   ├── hooks/
│   ├── lib/
│   ├── styles/
│   └── types/
└── public/
```

Once the Lovable scaffold lands here, the next step is wiring contracts (Cursor or Claude) per `docs/frontend-spec.md` against the deployment manifest at `deployments/arbitrumSepolia.json`.

The repo's root `.gitignore` already excludes `frontend/node_modules`, `frontend/dist`, `frontend/.vite`, and `frontend/coverage`.
